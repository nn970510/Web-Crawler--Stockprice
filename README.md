# Web-Crawler--Stockprice
A program uses to craw the real-time stock price.  
Write on Fedora.  
dl script will keep download WSJ stock data (Now the frequency is downloading per min and download 60 times then stop).
HW9.py will save data to MySQL DB and extract useful value.
Index will get data from DB and show on a webpage.  
Result pic shows what the webpage will look like.
